export default ({
  appVersion,
  dotaVersion,
  dotaVersionDate,
  wikiVersion,
  wikiVersionDate,
  wikiVersionFolder,
}) => ({
  appVersion,
  dotaVersion,
  dotaVersionDate,
  wikiVersion,
  wikiVersionDate,
  wikiVersionFolder,
})